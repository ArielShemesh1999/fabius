{
  "name": "fabius-runtime",
  "version": "1.0.0",
  "private": true,
  "description": "fabius, running locally: the routed brain, local hands, a permission gate, on-disk memory, keyless external recon, and a serverless channel.",
  "type": "module",
  "bin": { "fabius": "./fabius.mjs" },
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "vectors": "bash tools/fetch-vectors.sh",
    "doctor": "node fabius.mjs doctor"
  },
  "dependencies": {},
  "license": "UNLICENSED"
}
