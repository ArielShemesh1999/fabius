{
  "name": "fabius-runtime",
  "version": "1.0.0",
  "private": true,
  "description": "A zero-dependency local runner for the fabius rules — the same sealed rule files handed to a model through your own API key, with a permission gate, on-disk memory, keyless external recon, and a serverless channel.",
  "type": "module",
  "bin": { "fabius": "./fabius.mjs" },
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "vectors": "bash tools/fetch-vectors.sh",
    "doctor": "node fabius.mjs doctor"
  },
  "dependencies": {},
  "license": "UNLICENSED"
}
